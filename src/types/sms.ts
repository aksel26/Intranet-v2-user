export interface SmsDetials {
  smsMessageIdx: number;
  toPhoneNumber: string;
  status: string;
  sendAt: string;
  failureReason: any;
  fromPhoneNumber: string;
  message: string;
  createdAt: string;
  smsTag: string;
}
